import { Box } from '@/components';
import styled from 'styled-components';

export const StyledBox = styled(Box)`
    position: absolute;
    right: -15px;
    width: 15px;
    height: 66px;
    background-color: #fff;
    border-radius: 0px 5px 5px 0px !important;
    cursor: pointer;
    box-shadow: 3px 0px 8px 0px #00000026;
`;
